# Comments are provided throughout this file to help you get started.
# If you need more help, visit the Docker Compose reference guide at
# https://docs.docker.com/go/compose-spec-reference/

# Here the instructions define your application as a service called "server".
# This service is built from the Dockerfile in the current directory.
# You can add other services your application may depend on here, such as a
# database or a cache. For examples, see the Awesome Compose repository:
# https://github.com/docker/awesome-compose
services:
  postgres_db_container:
    image: postgres:17
    container_name: chacePostgres17
    restart: always
    environment:
      # POSTGRES_USER: postgres # optional
      POSTGRES_PASSWORD: postgre
      # POSTGRES_DB: mydb     # optional
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - 2345:5432


  server:
    build:
      context: .
    ports:
      - 8080:8080
    depends_on:
      - postgres_db_container

volumes:
  pgdata:

# The commented out section below is an example of how to define a PostgreSQL DB that your application can use. 
# `depends_on` tells Docker Compose to start the database before your application. 
# The `db-data` volume persists the database data between container restarts. 